// 比 webpack.config.js 多一个 plugin

var webpack = require('webpack');
var commonsPlugin = new webpack.optimize.CommonsChunkPlugin('common.js');

module.exports = {
    entry: 'app.js',
    output: {
        path: 'dist',
        filename: 'app.js'
    },
    plugins: [commonsPlugin, new webpack.optimize.MinChunkSizePlugin(minSize)]
};
